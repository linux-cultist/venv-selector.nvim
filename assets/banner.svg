<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg"
     width="1200" height="240" viewBox="0 0 1200 240" role="img" aria-label="venv-selector banner">
  <title>venv-selector.nvim</title>
  <defs>
    <linearGradient id="bg" x1="0" x2="1" y1="0" y2="1">
      <stop offset="0%" stop-color="#0f172a" />
      <stop offset="100%" stop-color="#0b1220" />
    </linearGradient>

    <filter id="shadow" x="-50%" y="-50%" width="200%" height="200%">
      <feDropShadow dx="0" dy="8" stdDeviation="16" flood-color="#000" flood-opacity="0.35"/>
    </filter>

    <style type="text/css"><![CDATA[
      .title { font: 700 36px/1.1 -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial; fill: #fff; }
      .subtitle { font: 400 16px/1.3 -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial; fill: #cbd5e1; }
      .badge { font: 600 13px/1 "Segoe UI", Roboto, "Helvetica Neue", Arial; fill: #0b1220; }
    ]]></style>
  </defs>

  <!-- Background -->
  <rect x="20" y="20" rx="16" ry="16" width="1160" height="200" fill="url(#bg)" filter="url(#shadow)"/>

  <!-- Left column: title + subtitle -->
  <g transform="translate(60,70)">
    <!-- Emoji circle background (optional subtle visual) -->
    <circle cx="28" cy="28" r="28" fill="#10b981" opacity="0.95"/>
    <text x="18" y="36" font-size="26" text-anchor="middle" fill="#062017" style="font-weight:700">🐍</text>

    <!-- Title -->
    <text class="title" x="80" y="24">🎉 venv-selector.nvim</text>

    <!-- Subtitle (wrap manually across two lines to avoid relying on external wrapping) -->
    <text class="subtitle" x="80" y="56">
      Discover and activate Python virtual environments inside Neovim — no restart required.
    </text>


</svg>