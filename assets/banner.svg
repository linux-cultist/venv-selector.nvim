<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg"
     width="1200" height="240" viewBox="0 0 1200 240" role="img" aria-labelledby="title desc">
  <title id="title">venv-selector.nvim — header</title>
  <desc id="desc">Compact header showing a green circle with a python emoji, the project title and a short subtitle.</desc>

  <defs>
    <linearGradient id="bgGrad" x1="0" x2="1" y1="0" y2="1">
      <stop offset="0%" stop-color="#071029" />
      <stop offset="100%" stop-color="#081427" />
    </linearGradient>

    <filter id="shadow" x="-50%" y="-50%" width="200%" height="200%">
      <feDropShadow dx="0" dy="8" stdDeviation="14" flood-color="#000" flood-opacity="0.35"/>
    </filter>

    <style type="text/css"><![CDATA[
      .panel { fill: url(#bgGrad); }
      .title { font: 700 40px/1.1 -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial; fill: #ffffff; }
      .subtitle { font: 400 16px/1.4 -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial; fill: #cbd5e1; }
    ]]></style>
  </defs>

  <!-- Background panel -->
  <rect x="24" y="24" rx="16" ry="16" width="1152" height="192" class="panel" filter="url(#shadow)"/>

  <!-- Left: green circle with emoji -->
  <g transform="translate(72,60)" aria-hidden="true">
    <circle cx="36" cy="36" r="36" fill="#10b981" />
    <text x="36" y="46" text-anchor="middle" font-size="32" fill="#042118" style="font-weight:700">🐍</text>
  </g>

  <!-- Title + subtitle -->
  <g transform="translate(140,74)">
    <text class="title" x="0" y="28">venv-selector.nvim</text>
    <text class="subtitle" x="0" y="60">Discover and activate Python virtual environments inside Neovim — no restart required.</text>
  </g>
</svg>